package com.amazon.emr.hadoop;

import org.apache.hadoop.conf.Configuration;
import org.apache.hadoop.io.BytesWritable;
import org.apache.hadoop.io.LongWritable;
import org.apache.hadoop.mapred.FileSplit;
import org.apache.hadoop.mapred.RecordReader;

import java.io.IOException;

/*
public class ConcatenatedCompressedRecordReader
    implements RecordReader<LongWritable, BytesWritable>
{
    public ConcatenatedCompressedRecordReader(Configuration job, FileSplit split)
        throws IOException
    {

    }
}
*/
